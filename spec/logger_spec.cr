require "./spec_helper"

describe "Logger" do
  it "logs stuff" do
    # IO.pipe do |r, w|
    #   logger = Kemal::Logger.new(w)
    #   logger.info "Info from logger"
    #   r.gets.should match(/Info from logger/)
    # end
  end
end
